# 社区运营系统 — 场地管理模块 PRD

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.1(初稿) |
| 撰写日期 | 2026-09-01 |
| 所属系统 | 社区运营系统 |
| 所属模块 | 场地管理 |
| 原型文件 | framework.html / venue-config.html / venue-schedule.html / venue-calendar.html / venue-detail.html / venue-orders.html |
| 文档状态 | 待评审(含待确认事项,见第 8 章) |

---

## 1. 背景与目标

社区内公共场地(羽毛球馆、舞蹈排练室、多功能会议室等)需要统一管理。本模块为运营平台中的场地管理模块,提供场地档案配置、排期管理、预订订单查看三个能力,支撑员工 App 端、居民 App 端的场地预订业务。

**目标:**
- 活动管家可以在运营平台完成场地的建档、维护与停用;
- 活动管家可以按日/周/月批量创建场地排期,并对过期/无用排期进行删除;
- 后台可查看 App 端产生的预订订单及签到情况,订单操作(签到/取消)由 App 端完成。

**非目标(本期不做):**
- 不做订单审批流;
- 不做数据导出(Excel)、报表统计;
- 不涉及支付环节(订单状态体系为"签到视角",非"支付视角")。

---

## 2. 角色与权限

| 角色 | 端 | 职责 |
| --- | --- | --- |
| 活动管家 | 运营平台(PC 后台) | 本模块唯一操作角色:场地配置、排期管理、订单查看 |
| 员工 | 员工 App | 浏览可预订排期,发起场地预订,签到/取消订单 |
| 居民(客户) | 居民 App | 浏览可预订排期,发起场地预订,签到/取消订单 |

> 运营平台侧仅活动管家可操作本模块;员工/居民通过各自 App 端预订,App 端交互不在本 PRD 范围内。

---

## 3. 核心业务模型

### 3.1 概念关系

```
场地(基础配置) ──1:N──> 排期(某天某时段) ──1:N──> 预订订单(员工/客户)
                                  ──1:1──> 课程订单(活动管家排课自动生成,占满)
```

- **一个场地**可配置多个排期(同一天可有多个时段排期);
- **一个排期**可被多个员工/客户订单占用(按预订人数累加),也可被一个课程排课整段占满;
- **排期是容量载体**:排期容量 = 场地容纳人数;订单是容量占用方。

### 3.2 预订类型

场地可配置预订类型(可多选,至少一项):

| 类型 | 说明 | 容量占用方式 |
| --- | --- | --- |
| 员工 | 员工 App 端预订 | 按订单预订人数累加 |
| 客户 | 居民 App 端预订 | 按订单预订人数累加 |
| 课程 | 活动管家在运营平台排课产生 | **默认占满全部名额**,不再接受员工/客户预订 |

### 3.3 排期状态

| 状态 | 定义 | 展示 |
| --- | --- | --- |
| 可预订 | 有剩余名额,且在可预订时间范围内 | 蓝色标签,展示 预订人数/容纳人数 |
| 已约满 | 名额已占满(含课程占用) | 红色标签,展示 预订人数/容纳人数 |
| 不可预订 | 排期时段已过期(结束时间早于当前时间)或场地已停用 | 灰色标签 |

> 说明:存在预订订单的排期**不得**展示为"不可预订",应根据名额情况展示"可预订"或"已约满";系统无"锁定"状态。

### 3.4 订单状态

| 状态 | 定义 |
| --- | --- |
| 未签到 | 订单已提交并占用名额,等待用户到场签到 |
| 已签到 | 用户到场完成签到核销 |
| 已取消 | 订单被取消(名额释放) |

---

## 4. 业务规则

### 4.1 预订规则(App 端下单约束,后台排期展示同步体现)

1. **时间规则**:当前时间在"排期结束时间前 15 分钟"以内或已超过排期结束时间的,不可预订;
2. **类型互斥规则**:排期被课程占用时,不允许"客户""员工"预订(课程默认占满,名额为 0);
3. **容量规则**:客户、员工订单的预订人数总和,不可超过排期的容纳人数。

### 4.2 排期规则(后台)

- 排期由活动管家手动创建,支持**按日 / 按周 / 按月**批量创建;
- 一个排期可包含**多个时段**,时段粒度为 **30 分钟**,多个时段之间**不允许重叠**;
- 排期**不配置**排期名称、备注、预订类型(预订类型由订单决定);
- 场地开放时间(如"开放至 20:00")由活动管家按需求配置,作为排期时段的约束(具体交互见待确认事项);
- **删除排期**的前提:
  1. 该排期未被任何订单预订;
  2. 排期结束时间大于当前时间。
  两个条件同时满足才允许删除,且仅支持单个排期删除(月历视图日详情弹窗内操作)。

### 4.3 场地规则(基础配置)

- **场地编号**由后台系统在新增提交后自动生成,新增表单不展示该字段;编辑时编号反显、不可编辑;
- **楼栋、楼层、房间号**为必填;
- 场地状态仅两档:**开放中 / 停用**(无"维护中");
- **停用约束**:场地存在"已预订未签到"的排期时,不可停用;
- 编辑时状态反显、不可编辑(状态的变更仅通过列表/详情页的 停用/开放 操作完成)。

### 4.4 订单规则

- 订单无审批流,满足 4.1 预订规则即可下单,**下单即占用名额**;
- 订单的**签到、取消操作在 App 端完成**,运营后台仅提供查询与详情查看(订单详情抽屉底部提示:"本后台仅提供订单查询,如需取消订单或到场签到,请在 App 端进行相应操作。");
- 排期状态与订单联动:
  - 订单占用名额变化时,排期的 预订人数/容纳人数 实时更新,并触发"可预订 ⇄ 已约满"状态流转;
  - 订单取消后释放名额。

---

## 5. 功能需求

### 5.1 基础配置(原型:venue-config.html / venue-detail.html)

菜单名:**基础配置**(位于"场地管理"菜单组最后一项)。

#### 5.1.1 场地列表

- 顶部统计区:场地总数、开放中数量等概览;
- 筛选区:关键字(场地名称/编号)等;
- 列表字段(从左至右,无复选框):

| 字段 | 说明 |
| --- | --- |
| 场地编号 | 系统自动生成,如 CD-001 |
| 场地名称 | |
| 照片 | 缩略图展示 |
| 楼栋 / 楼层 / 房间号 | 必填字段 |
| 容纳人数 | 数值 |
| 预订类型 | 员工/客户/课程,多选,标签组展示 |
| 状态 | 开放中(绿)/ 停用(灰) |
| 场地描述 | 超长省略展示 |
| 创建人 / 创建时间 | |
| 最新操作人 / 最新操作时间 | |
| 操作 | 详情 / 编辑 / 停用(或 开放) |

- 列表支持横向滚动(字段较多);
- **停用**:二次确认弹窗;若该场地存在已预订未签到的排期,阻断并 Toast 提示"「场地名」存在已预订未签到的排期,不可停用";停用后行置灰,操作列变为"开放";
- **开放**:停用场地可重新开放。

#### 5.1.2 新增 / 编辑场地(弹窗)

- 新增与编辑共用同一弹窗;
- 字段:场地名称*、楼栋*、楼层*、房间号*、容纳人数*、预订类型*(多选,至少一项)、照片*、场地描述;
- **新增时不展示场地编号**(提交后由后台生成);
- **编辑时**:场地编号反显不可编辑;状态反显不可编辑;
- 照片:支持多张上传,第一张为封面,限制单张大小与数量(建议 jpg/png,≤5MB,≤6 张,具体数值待确认)。

#### 5.1.3 场地详情(页面)

- 只读反显全部字段:基本信息(编号、名称、状态、容纳人数、楼栋/楼层/房间号、预订类型)、照片墙(首张标注封面)、场地描述、系统信息(创建人/创建时间/最新操作人/最新操作时间);
- 页脚:返回列表 / 编辑(唤起编辑弹窗)。

### 5.2 场地排期(原型:venue-schedule.html / venue-calendar.html)

菜单名:**场地排期**。包含两个视图:

#### 5.2.1 日视图(venue-schedule.html)

- **日期必选在先**:顶部日期选择器(日期输入 + 前后翻日 + "今天" + 本周快捷日期),选定日期后展示该日各场地的排期;
- 概览条:场地数 / 排期数 / 已订人数,及图例(可预订、已约满、未排期);
- **按场地分组展示单日排期卡片**,一个场地一天可有多个排期,逐条列出:
  - 排期时段、排期状态(可预订/已约满)、订单类型标签(员工/客户/课程)、容量进度(预订人数/容纳人数 + 进度条);
  - 排期卡内嵌**订单明细行**:订单类型、预订人姓名/电话、人数、签到状态(已签到/待签到);
- 未被预订的排期同样展示(空排期卡:"暂无预订订单,可预订");
- 特殊场景:停用场地无排期,展示说明。

#### 5.2.2 月历视图(venue-calendar.html)

- 从日视图或场地列表"排期"按钮进入,**按单个场地**展示整月排期;
- 月历网格(周一起始),日期格内以色块标签展示当天排期:员工预订 / 客户预订 / 课程(默认占满)/ 已约满 / 排期未预订(可订);
- 点击某天弹出**日详情弹窗**,列出当天各排期(时段、状态、容量、关联订单明细);
- 日详情弹窗内支持**删除单个排期**(需满足 4.2 删除前提,不满足则不出现删除入口);
- 顶部:场地信息条 + 月份切换 + "＋ 新增排期"按钮。

#### 5.2.3 新增排期(弹窗)

- 排期日期模式:**按日 / 按周 / 按月** 三种批量方式切换;
  - 按日:选择单个/多个日期;
  - 按周:选择星期(每周固定);
  - 按月:选择每月几号;
- 排期时段:支持添加**多个时段**,开始/结束时间下拉粒度 **30 分钟**(08:00–22:00);
- 校验:
  - 结束时间必须大于开始时间;
  - 多个时段之间不允许重叠;
- 展示匹配提示(本次批量将生成的排期概览)。

### 5.3 订单列表(原型:venue-orders.html)

菜单名:**订单列表**。

- **状态 Tab(分段控件样式)**:未签到 / 已签到 / 已取消,Tab 上展示数量角标;点击 Tab 真实过滤列表;签到/取消等状态变更由 App 端产生,后台 Tab 计数随数据变化;
- 筛选区:关键字、场地、预订类型、状态、下单日期区间 + 查询/重置(不做导出);
- 列表字段:

| 字段 | 说明 |
| --- | --- |
| 订单号 | 如 YY20260901-0003 |
| 场地 / 排期时段 | 场地名称 + 具体排期日期时段 |
| 预订类型 | 员工(蓝)/ 客户(绿)/ 课程(紫) |
| 预订人 / 电话 | |
| 预订人数 | 参与人数 |
| 状态 | 未签到(橙)/ 已签到(绿)/ 已取消(灰) |
| 下单时间 | |
| 操作 | 详情 |

- **详情抽屉**(右侧滑出,宽 600px):
  - 顶部状态横幅(如"状态:待签到,请于排期时段开始前到场签到");
  - **关联排期**:场地(含编号/位置)、排期时段、容纳人数、排期状态(已约满 + 预订人数 8/8);
  - **预订信息**:预订类型、预订人、联系电话、预订人数、下单时间、订单备注;
  - **状态流转**时间线:提交订单占用名额(含人数)→ 到场签到核销 → 排期结束订单完成;
  - 底部提示:"本后台仅提供订单查询,如需取消订单或到场签到,请在 App 端进行相应操作。";
  - 抽屉**无操作按钮**(无取消订单/签到核销)。

---

## 6. 状态流转图

### 6.1 排期状态

```
           名额被订单占满 / 课程占用
可预订 ────────────────────────────> 已约满
   ▲                                   │
   └───────── 订单取消释放名额 ─────────┘

可预订/已约满 ── 结束时间早于当前时间 ──> 不可预订(过期)
任意状态     ── 所属场地被停用(无未签到排期) ──> 不可预订(停用)
```

### 6.2 订单状态

```
未签到 ── App端签到 ──> 已签到
未签到 ── App端取消 ──> 已取消(释放名额)
```

### 6.3 场地状态

```
开放中 ── 停用(存在已预订未签到排期则阻断) ──> 停用
停用   ── 开放 ──> 开放中
```

---

## 7. 页面清单与原型对照

| 序号 | 页面 | 原型文件 | 入口 |
| --- | --- | --- | --- |
| 1 | 整体框架 | framework.html | — |
| 2 | 基础配置-列表 | venue-config.html | 菜单"基础配置" |
| 3 | 基础配置-详情 | venue-detail.html | 列表"详情" |
| 4 | 场地排期-日视图 | venue-schedule.html | 菜单"场地排期" |
| 5 | 场地排期-月历视图 | venue-calendar.html | 日视图/列表"排期"按钮 |
| 6 | 订单列表 | venue-orders.html | 菜单"订单列表" |

---

## 8. 待确认事项

| 序号 | 事项 | 现状/假设 | 需确认 |
| --- | --- | --- | --- |
| 1 | 场地开放时间的配置交互 | 原型仅在排期卡片体现"开放至 20:00"文案 | 是否在基础配置中增加"每周开放日 + 开放时段"字段?排期时段超出开放时间是否阻断? |
| 2 | 订单取消规则 | PRD 仅说明取消在 App 端完成 | 取消是否设截止时间(如开始前 X 小时)?取消后名额释放规则确认 |
| 3 | 超时未签到处理 | 原型未涉及 | 排期结束后仍未签到的订单如何处理(自动取消/标记爽约/保持未签到)? |
| 4 | 预订时间窗口 | 规则为"结束前 15 分钟内不可订" | 是否同时限制最早可订时间(如仅开放未来 N 天的排期)? |
| 5 | 照片上传限制 | 原型提示:jpg/png、≤5MB、≤6 张、首张封面 | 数值与规则需确认 |
| 6 | 排期编辑 | 当前仅支持删除,不支持编辑 | 是否需要排期修改能力(改时段/扩容)?有订单的排期如何处理? |
| 7 | 员工/客户身份校验 | 场地配置预订类型决定 App 端可见性 | 员工与居民 App 的账号体系及校验方式由 App 端 PRD 定义,需对齐 |
| 8 | 批量排期的生效范围 | 按周/按月批量创建的终止条件 | 是否需要设置重复截止日期(如排至本学期末)? |

---

## 9. 修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-01 | 依据原型梳理初稿 | — |
